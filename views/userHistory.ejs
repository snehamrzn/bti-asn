<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About</title>
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />
    <title>Climate Solutions</title>
</head>
<body class="min-h-screen">
  <%- include('partials/navbar', {page: '/about'}) %>
      <div class="container mx-auto">
      <div class="hero bg-base-200 min-h-32 rounded-lg p-10">
        <div class="hero-content text-center">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold"><%=session.user.userName %></h1>
            <p class="py-6">
                User History:<%= session.user.email %>
            </p>
          </div>
        </div>
      </div>
      </div>
      <div class="container mx-auto">
        <table class ="table table-zebra">
            <thread>
                <tr>
                    <th>Login Date/Time </th>
                    <th>Client Information</th>
                </tr>
                <tbody>
                    <% session.user.loginHistory.forEach(function(userHistory) { %>
                        <tr>
                            <td><%= new Date(userHistory.dateTime).toLocaleString() %></td> 
                            <td><%= userHistory.userAgent %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </thread>
      </table>
        
      </div>
</body>
</html>